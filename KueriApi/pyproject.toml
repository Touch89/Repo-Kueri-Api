[project]
name = "kueriapi"
version = "0.1.0"
description = ""
authors = [
    {name = "Scorpion-CC"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard] (>=0.135.1,<0.136.0)",
    "sqlmodel (>=0.0.37,<0.0.38)"
]

[tool.poetry]
packages = [{include = "kueriapi", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
